var inputWidth;
var inputHeight;
inputWidth = prompt("nhập vào giá trị chiều Rộng:");
inputHeight = prompt("nhập vào giá trị chiều Cao:");
var width = parseInt(inputWidth);
var height = parseInt(inputHeight);
var area = width * height;
document.write("Kết quả là: " + area);
